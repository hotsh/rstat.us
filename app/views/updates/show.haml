= render "updates/_detail", :locals => {:update => @update}

-unless @update.referral.nil?
  .reply
    .title
      in reply to
    = render "updates/_detail", :locals => {:update => @update.referral, :reply => true}
