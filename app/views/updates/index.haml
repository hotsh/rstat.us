- title "Updates"

- content_for :top do
  - unless current_user.nil?
    = render "updates/_form"

  #updates-menu-wrap
    #updates-menu-background
      = render "shared/menu/_updates"

= render "updates/_list", :locals => {:updates => @updates}
