!!! 5
%html.no-js
  != haml :_head
  %body#update
    - if flash[:notice] || flash[:error]
      #flash
        = flash[:notice]
    
    .single-update.update
      != yield           
    != haml :'updates/footer'