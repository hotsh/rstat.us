!!! 5
%html.no-js
  != haml :_head, :locals => {:css => ['update']}
  %body#update
    - if flash[:notice] || flash[:error]
      #flash
        = flash[:notice]
    
    .single-update
      != yield           
    != haml :'updates/footer'