- nav = [{:url => "/", :name => "Timeline"}, {:url => "/replies", :name => "Replies"}]
#navigation
  %ul
    - nav.each do |item|
      %li{:class => request.path_info == item[:url] ? "active" : ""}
        %a{:href => item[:url]}
          = item[:name]
